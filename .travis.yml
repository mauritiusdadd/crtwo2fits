language: python

os:
  - linux

sudo: off

addons:

  apt:
    packages:
      - python-numpy
      - python-scipy
      - gettext

python:
  - "3.4"

# command to install dependencies
install:
  - "pip install astropy"
  - "./configure"
  - "python setup.py install"

script:
  - which crtwo2fits
